export enum SoundEffect {
    EatingSound,
    GameOverSound,
}

export enum Direction {
    Up,
    Down,
    Left,
    Right,
    Null
}

export enum GameState {
    StartingScreen,
    Running,
    Stopped
}
