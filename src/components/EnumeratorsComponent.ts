export enum SoundEffect {
    EatingSound,
    GameOverSound,
    VictorySound,
    RecordPassedSound
}

export enum Direction {
    Up,
    Down,
    Left,
    Right,
    Null
}

export enum GameState {
    StartingScreen,
    Running,
    Stopped
}
